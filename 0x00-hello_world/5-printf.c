#include<stdio.h>
/**
 *main - Uses printf to print a string
 *
 *Description: use printf
 *Return: Should be zero (0)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
